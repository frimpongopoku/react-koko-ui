import React from "react";
import Radio from "./../form generator/radio-group/RadioGroup";
export default {
  title: "Radio",
  component: Radio,
};

const Template = (args) => <Radio {...args} />;

export const Default = Template.bind({});
